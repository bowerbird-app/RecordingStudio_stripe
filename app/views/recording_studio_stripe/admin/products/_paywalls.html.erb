<% paywalls = RecordingStudioStripe::Paywall.order(:name) %>
<% if paywalls.any? %>
  <% paywalls.each do |paywall| %>
    <%= render FlatPack::Checkbox::Component.new(
      name: "paywall_names[]",
      value: paywall.name,
      checked: Array(@selected_paywall_names).include?(paywall.name),
      label: paywall.label
    ) %>
  <% end %>
<% end %>
