<% recording_studio_page_nav(title: "Plans", page_nav_back_url: main_app.root_path, page_nav_back_label: "Home") %>

<%= render RecordingStudioStripe::PlansComponent.new(
  products: @products,
  interval: @interval,
  subscription: @current_subscription,
  align: :left,
  monthly_href: url_for(interval: "month"),
  yearly_href: url_for(interval: "year")
) %>
